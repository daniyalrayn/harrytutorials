
const $=s=>document.querySelector(s);
document.querySelector('.menu')?.addEventListener('click',()=>document.querySelector('.links').classList.toggle('open'));
document.querySelectorAll('.count').forEach(el=>{const n=+el.dataset.to||0;let x=0;const t=setInterval(()=>{x+=Math.ceil(n/35);if(x>=n){x=n;clearInterval(t)}el.textContent=x},35)});
function startQuiz(){const qs=[...document.querySelectorAll('.question')],bar=$('.progress i'),res=$('.quizResult');let n=0,score=0;function show(){qs.forEach((q,i)=>q.classList.toggle('active',i===n));bar.style.width=((n+1)/qs.length*100)+'%'}window.answer=ok=>{if(ok)score++;n++;if(n<qs.length)show();else{qs.forEach(q=>q.classList.remove('active'));bar.style.width='100%';res.hidden=false;res.innerHTML=`<div class="score">${score}/${qs.length}</div><h2>${score>=4?'Excellent! 🔥':'Good start! 💪'}</h2><p>${score>=4?'Strong start. Keep building that preparation.':'There is room to improve — that is exactly what practice is for.'}</p><a class="btn" href="contact.html">Get a Study Plan →</a>`}};show()}
function enquiry(e){e.preventDefault();const f=e.target;const subject=encodeURIComponent('Harry Tutorials Enquiry - '+f.name.value);const body=encodeURIComponent(`Name: ${f.name.value}\nClass: ${f.class.value}\nPhone: ${f.phone.value}\nMessage: ${f.message.value}`);location.href=`mailto:harishr4447@gmail.com?subject=${subject}&body=${body}`}
